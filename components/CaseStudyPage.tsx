/**
 * Placeholder for an individual project's case study page. None of these
 * have real content yet — just an empty black page for now, deliberately
 * not reusing the /work page's (light) Navbar/Footer shell since that's
 * the old design, not this page's eventual look.
 */
export default function CaseStudyPage() {
  return <div className="min-h-screen w-full bg-black" />;
}

import { ShapeIcon } from "@/components/icons/ShapeIcon";

/** "curiosity" chip icon (Figma 203:1948) — see ShapeIcon for why this
 * renders a real vector letterform-grid asset instead of animated squares now. */
export default function CuriosityIcon() {
  return <ShapeIcon name="curiosity" />;
}

import { ShapeIcon } from "@/components/icons/ShapeIcon";

/** "painter" chip icon (Figma 203:1948) — see ShapeIcon for why this
 * renders a real vector letterform-grid asset instead of animated bars now. */
export default function BarChartIcon() {
  return <ShapeIcon name="painter" />;
}

import { ShapeIcon } from "@/components/icons/ShapeIcon";

/** "deep respect" chip icon (Figma 203:1948) — see ShapeIcon for why this
 * renders a real vector letterform-grid asset instead of an animated equals sign
 * now. */
export default function EqualsIcon() {
  return <ShapeIcon name="deep-respect" />;
}

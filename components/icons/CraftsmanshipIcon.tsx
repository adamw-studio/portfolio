import { ShapeIcon } from "@/components/icons/ShapeIcon";

/** "craftsmanship" chip icon (Figma 203:1948) — see ShapeIcon for why this
 * renders a real vector letterform-grid asset instead of animated bars now. */
export default function CraftsmanshipIcon() {
  return <ShapeIcon name="craftsmanship" />;
}
